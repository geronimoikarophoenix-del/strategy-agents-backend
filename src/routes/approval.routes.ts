import { Router } from 'express';

const router = Router();

// GET /api/approvals/:portfolio_id/pending - Get pending approvals
// POST /api/approvals/:approval_id/approve - Approve alert
// POST /api/approvals/:approval_id/reject - Reject alert

export default router;
